<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MotorcycleAlertReceiver" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- App background - DARK GREY -->
        <item name="android:windowBackground">@color/darkGrey</item>

        <!-- Force black status and navigation bars -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>

        <!-- Primary colors -->
        <item name="colorPrimary">@color/greyDark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Surface / cards -->
        <item name="colorSurface">@color/darkSurface</item>
        <item name="colorOnSurface">@color/white</item>

        <!-- Force ALL text to be white -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/textLightDark</item>

        <!-- Override any tint colors -->
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/white</item>

    </style>
</resources>